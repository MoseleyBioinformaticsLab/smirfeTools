context("test-assignments.R")

# test_that("filtering assignments works", {
#   data(assigned_data)
#   tmp <- filter_assignments(assigned_data[[1]]$assignments)
#   expect_equal(sum(tmp$Type %in% "Secondary"), 0)
# })
